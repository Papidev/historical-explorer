# Issue tracker

Historical Explorer tracks work in GitHub Issues in `Papidev/historical-explorer`.

- Use `gh issue view <number> --comments` to read an issue and its discussion.
- Use `gh issue list` with appropriate state and label filters to find issues.
- Infer the repository from the current checkout when running `gh` commands.

When a skill says to fetch the relevant ticket, read the referenced GitHub issue.
